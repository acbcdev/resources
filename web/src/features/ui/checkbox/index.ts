import Checkbox, { checkbox, checkboxWrapper, checkboxLabel, checkboxIcon } from "./Checkbox.astro";

const CheckboxVariants = { checkbox, checkboxWrapper, checkboxLabel, checkboxIcon };

export { Checkbox, CheckboxVariants };

export default Checkbox;
