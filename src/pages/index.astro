---
import { Button } from '@/features/ui/button';
import { categories } from '@/features/common/consts';
import { DATA } from '@/features/common/data/index';
import Layout from '@/layouts/Layout.astro';
import { slugify, toHundreds } from '@/features/common/lib/utils';
import { TARGETAUDIENCE } from '@/features/audiences/types/audience';

console.log(DATA.length);
const numOfResources = toHundreds(DATA.length);
const filterCate = categories.filter((category) => category.length);
---

<!-- TODO Add METADATA -->
<Layout
	title={`Resources ${numOfResources}+ | Tools | Libraries | Illustrations | And More`}
	accompanimentTitle={`${numOfResources}+`}
/>
