import Radio, { radio, radioWrapper, radioLabel, radioIndicator, radioDot } from "./Radio.astro";
import RadioGroup, { radioGroup, radioGroupLabel } from "./RadioGroup.astro";

const RadioVariants = { radio, radioWrapper, radioLabel, radioIndicator, radioDot, radioGroup, radioGroupLabel };

export { Radio, RadioGroup, RadioVariants };

export default Radio;
